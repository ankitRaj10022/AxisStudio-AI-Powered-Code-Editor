apiVersion: v1
kind: Service
metadata:
  name: axisstudio-web
  namespace: axisstudio
  labels:
    app.kubernetes.io/name: axisstudio
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: axisstudio
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
